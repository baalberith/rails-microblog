- if @user.errors.any?
  %div#error_explanation
    %h4
      = pluralize @user.errors.count, "error"
      prohibited this user form being saved
    %p There were problems with the following fields:
    %ul
      - @user.errors.full_messages.each do |msg|
        %li= msg
